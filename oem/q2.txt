SELECT
  TARGET_NAME,
  TABLESPACE_NAME,
  ROUND((TABLESPACE_USED_SIZE / TABLESPACE_SIZE) * 100, 2) AS USED_PERCENT
FROM
  MGMT$DB_TABLESPACES
WHERE
  TABLESPACE_USED_SIZE IS NOT NULL
  AND TABLESPACE_SIZE IS NOT NULL
  AND (TABLESPACE_USED_SIZE / TABLESPACE_SIZE) * 100 > 85;
